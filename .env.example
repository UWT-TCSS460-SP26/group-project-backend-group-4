PORT=3000
TMDB_API_KEY=<YOUR TMDB API KEY>
DATABASE_URL=postgresql://tcss460_user:CHANGE_ME@localhost:5432/tcss460_db
AUTH_ISSUER=https://tcss-460-iam.onrender.com
API_AUDIENCE=group-4-api
CORS_ALLOWED_ORIGINS=http://localhost:3000, http://localhost:3001

# Optional, only needed if you want to receive discord notifications via webhook whenever an issue is created
# The public url for the issue tracker, only used for creating quick links in the webhook
ISSUE_TRACKER_URL=http://localhost:3001
# The actual webhook url to post messages to
DISCORD_ISSUE_NOTIFICATION_WEBHOOK=<YOUR DISCORD WEBHOOK URL>